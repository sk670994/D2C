# AdSpy v20

Focused fix for the v19 diagnostics.

- Defines the missing `proxyMediaUrl()` used by the inspection image fallback.
- Addresses the `FILTERS` runtime-value-only-used-as-type warning.
- Removes the stale `FileText` and `Play` imports from the creative modal.

No UI/UX, search, collection, or animation changes are included.
