# AdSpy v25

Build fixes:
- `searchPageId` is explicitly scoped inside `fetchSearch`.
- The malformed over-escaped Tailwind selector in `adspy-refinement-v23.css` is corrected.
- A clean final override stylesheet is provided without CSS parser hazards.

No new provider/API behavior is introduced.
